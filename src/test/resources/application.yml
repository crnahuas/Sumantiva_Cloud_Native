spring:
  datasource:
    url: jdbc:h2:mem:sumativa-test;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create-drop

app:
  storage:
    efs-path: ./target/test-efs
